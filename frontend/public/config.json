{
  "_comment": "Runtime public-tile config, read once at page load (see src/config.ts). Edit this file on the static host to change the tile origin without rebuilding the bundle. Empty tilesBase uses the page origin. Public tiles may use a CDN because tile requests contain no route data. Serve this file with a short or zero cache TTL so changes take effect on the next page load.",
  "tilesBase": ""
}
